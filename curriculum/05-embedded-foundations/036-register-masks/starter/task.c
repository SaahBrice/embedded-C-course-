#include "task.h"

/* TODO — Manipulate Register Bits: implement the declared interface.
 * Contract to prove: The field value is validated against the shifted mask before a clear-and-set update preserves all unrelated bits.
 */
