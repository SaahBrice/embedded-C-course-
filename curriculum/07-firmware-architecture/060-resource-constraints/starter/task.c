#include "task.h"

/* TODO — Budget Firmware Resources: implement the declared interface.
 * Contract to prove: Static queue storage is calculated with an overflow check before a RAM budget accepts it.
 */
