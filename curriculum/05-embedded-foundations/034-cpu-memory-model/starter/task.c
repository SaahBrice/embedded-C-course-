#include "task.h"

/* TODO — Map CPU and Memory Responsibilities: implement the declared interface.
 * Contract to prove: The returned value models the CPU's load-modify phase while preserving bits outside the requested masks.
 */
