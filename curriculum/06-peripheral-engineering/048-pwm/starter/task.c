#include "task.h"

/* TODO — Control Output with PWM: implement the declared interface.
 * Contract to prove: Widening prevents multiplication overflow, 0 and 100 percent map to the endpoints, and invalid percentages are rejected.
 */
