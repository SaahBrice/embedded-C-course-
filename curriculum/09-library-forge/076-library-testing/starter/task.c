#include "task.h"

/* TODO — Build a Library Test Matrix: implement the declared interface.
 * Contract to prove: Tests can state and exercise empty, full, wrapped, and invalid ring-buffer invariants independently of implementation.
 */
