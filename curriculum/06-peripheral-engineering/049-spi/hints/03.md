# Hint 3 — Concrete route for Transact over SPI

Keep the assessed interface fixed and use full duplex to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
