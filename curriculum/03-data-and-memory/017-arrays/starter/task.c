#include "task.h"

/* TODO — Work Safely with Arrays: implement the declared interface.
 * Contract to prove: The first live element seeds both extrema, and every later index is proven smaller than `count`.
 */
