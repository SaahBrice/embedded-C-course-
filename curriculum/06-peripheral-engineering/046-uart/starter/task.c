#include "task.h"

/* TODO — Build a UART Boundary: implement the declared interface.
 * Contract to prove: `uart_line_feed` accepts one byte per call, always preserves termination, reports a complete line at newline, and rejects overflow.
 */
