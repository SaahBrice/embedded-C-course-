#include "task.h"

/* TODO — Capstone: Serialize Deterministically: implement the declared interface.
 * Contract to prove: `record_serialize` emits an explicit version byte and little-endian fields, independent of structure padding and host byte order.
 */
