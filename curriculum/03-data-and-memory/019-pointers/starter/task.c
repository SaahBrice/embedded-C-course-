#include "task.h"

/* TODO — Read Pointer Relationships: implement the declared interface.
 * Contract to prove: A pointer and element count share one provenance-safe contract: null is accepted only for zero elements, impossible byte extents are rejected, and iteration never compares unrelated pointers.
 */
