#include "task.h"

/* TODO — Design Defensive APIs: implement the declared interface.
 * Contract to prove: Null pointers are legal only for a zero-length operation, capacity is checked before the first write, and failure leaves storage untouched.
 */
