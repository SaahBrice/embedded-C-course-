# Full solution: Build Your First C Program

This worked implementation demonstrates translation, main, standard output. Compare its observable behavior and boundaries with yours; different code is valid when it preserves the same contract.

```c
#include <stdio.h>
int main(void) { puts("firmware ready"); return 0; }
```
