#include "task.h"

/* TODO — Apply const and volatile Correctly: implement the declared interface.
 * Contract to prove: The pointer prevents writes through this view while volatile preserves the observable register read.
 */
