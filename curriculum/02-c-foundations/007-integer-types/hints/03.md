# Hint 3 — Concrete route for Choose Integer Types Deliberately

Keep the assessed interface fixed and use integer promotion to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
