# Hint 3 — Concrete route for Apply const and volatile Correctly

Keep the assessed interface fixed and use optimization to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
