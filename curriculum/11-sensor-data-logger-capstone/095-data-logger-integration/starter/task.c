#include "task.h"

/* TODO — Capstone: Integrate the Logger: implement the declared interface.
 * Contract to prove: The integrated logger validates before commit, timestamps and queues accepted samples, persists each record once, retries transport within a bound, and retains ownership after failure.
 */
