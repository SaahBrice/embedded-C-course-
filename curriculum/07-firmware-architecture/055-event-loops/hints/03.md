# Hint 3 — Concrete route for Build a Cooperative Event Loop

Keep the assessed interface fixed and use bounded work to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
