# Hint 3 — Concrete route for Drive Digital I/O

Keep the assessed interface fixed and use active low to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
