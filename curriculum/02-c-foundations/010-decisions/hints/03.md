# Hint 3 — Concrete route for Route Fault States

Keep the assessed interface fixed and use enumerated states to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
