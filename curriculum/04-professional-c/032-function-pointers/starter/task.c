#include "task.h"

/* TODO — Dispatch with Function Pointers: implement the declared interface.
 * Contract to prove: Dispatch validates table bounds and the selected callback before invoking it with the caller-owned context pointer.
 */
