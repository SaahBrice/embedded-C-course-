#include "task.h"

/* TODO — Model a Sensor Packet: implement the declared interface.
 * Contract to prove: The enum is the union tag: code reads only the member selected by a recognized tag and rejects every unknown value.
 */
