id = "controller-release"
title = "Release: Portable Sensor Controller"
version = "2.0.0"
order = 610
stage = 7
activity = "project"
duration_minutes = 50
platform = "host"
prerequisites = ["resource-constraints"]
objectives = ["Explain state machine in an embedded C context", "Apply event loop with explicit boundary behavior", "Produce and interpret executable engineering evidence"]
starter = "starter"
hints = ["hints/01.md", "hints/02.md", "hints/03.md"]

[content]
briefing = "briefing.md"
lesson = "lesson.md"
debrief = "debrief.md"
solution = "solution.md"

[[checks]]
id = "immutable-c-contract"
name = "Strict build and immutable behavior tests"
type = "c_harness"
sources = ["task.c"]
harness = "checks/test.c"
cflags = ["-std=c11", "-Wall", "-Wextra", "-Wpedantic", "-Werror", "-O0", "-g"]
timeout = 20
required = true
