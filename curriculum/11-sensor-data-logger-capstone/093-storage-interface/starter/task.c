#include "task.h"

/* TODO — Capstone: Persist Records: implement the declared interface.
 * Contract to prove: The orchestration retains ownership until every byte is acknowledged, supports valid partial writes, and stops on zero progress.
 */
