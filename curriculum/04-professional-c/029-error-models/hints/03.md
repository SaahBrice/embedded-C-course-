# Hint 3 — Concrete route for Propagate Errors Without Guessing

Keep the assessed interface fixed and use error context to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
