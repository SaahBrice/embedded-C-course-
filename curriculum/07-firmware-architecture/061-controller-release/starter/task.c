#include "task.h"

/* TODO — Release: Portable Sensor Controller: implement the declared interface.
 * Contract to prove: The controller release combines wrap-safe scheduling, bounded sample acceptance, and an explicit state update that counts only ready measurements at a due release.
 */
