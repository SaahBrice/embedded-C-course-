#include "task.h"

/* TODO — Define a Hardware Abstraction Boundary: implement the declared interface.
 * Contract to prove: Portable policy depends only on injected operations and context; sensor failure drives the output to its documented safe alarm state.
 */
