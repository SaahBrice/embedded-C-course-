#include "task.h"

/* TODO — Release: Binary Packet Parser: implement the declared interface.
 * Contract to prove: The parser release validates the whole frame header, decodes little-endian payload bytes without alignment assumptions, and commits a typed packet only after both stages succeed.
 */
