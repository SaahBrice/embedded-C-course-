# Hint 3 — Concrete route for Measure Floating-Point Trade-offs

Keep the assessed interface fixed and use fixed point to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
