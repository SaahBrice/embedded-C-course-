#ifndef LEARN_POINTERS_TASK_H
#define LEARN_POINTERS_TASK_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

bool checked_sum(const int32_t *values, size_t count, int64_t *out_sum);

#endif
