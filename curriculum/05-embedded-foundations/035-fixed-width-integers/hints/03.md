# Hint 3 — Concrete route for Use Fixed-Width Integers

Keep the assessed interface fixed and use format macros to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
