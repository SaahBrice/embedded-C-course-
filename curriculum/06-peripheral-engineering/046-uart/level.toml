id = "uart"
title = "Build a UART Boundary"
version = "2.0.0"
order = 460
stage = 6
activity = "simulate"
duration_minutes = 50
platform = "sim"
prerequisites = ["nonblocking-time"]
objectives = ["Explain baud in an embedded C context", "Apply TX/RX with explicit boundary behavior", "Produce and interpret executable engineering evidence"]
starter = "starter"
hints = ["hints/01.md", "hints/02.md", "hints/03.md"]

[content]
briefing = "briefing.md"
lesson = "lesson.md"
debrief = "debrief.md"
solution = "solution.md"

[[checks]]
id = "immutable-c-contract"
name = "Strict build and immutable behavior tests"
type = "c_harness"
sources = ["task.c"]
harness = "checks/test.c"
cflags = ["-std=c11", "-Wall", "-Wextra", "-Wpedantic", "-Werror", "-O0", "-g"]
timeout = 20
required = true
support_sources = ["platforms/host/src/sim_uart.c"]
support_include_dirs = ["platforms/host/include"]
