#include "task.h"

/* TODO — Harden Arithmetic: implement the declared interface.
 * Contract to prove: `checked_scale` performs multiplication in a wider defined type and narrows only after both signed limits are proven.
 */
