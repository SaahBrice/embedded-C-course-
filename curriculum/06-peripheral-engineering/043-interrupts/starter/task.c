#include "task.h"

/* TODO — Keep Interrupt Work Bounded: implement the declared interface.
 * Contract to prove: The interrupt callback performs one bounded mailbox write, never overwrites a pending event, and foreground code explicitly consumes ownership.
 */
