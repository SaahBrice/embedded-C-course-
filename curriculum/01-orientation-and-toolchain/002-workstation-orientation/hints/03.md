# Hint 3 — Concrete route for Implement Your First Firmware Function

Keep the assessed interface fixed and use unsigned status to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
