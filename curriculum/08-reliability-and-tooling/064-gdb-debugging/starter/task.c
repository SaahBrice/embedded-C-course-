#include "task.h"

/* TODO — Diagnose a Firmware Fault with GDB: implement the declared interface.
 * Contract to prove: The repaired loop touches exactly the live array range; a watchpoint would show only matching elements being changed.
 */
