#include "task.h"

/* TODO — Model Memory-Mapped I/O: implement the declared interface.
 * Contract to prove: The volatile register is read once and written once; the pure mask calculation between those observable accesses is explicit.
 */
