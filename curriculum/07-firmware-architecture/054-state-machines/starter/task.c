#include "task.h"

/* TODO — Model Behavior as States: implement the declared interface.
 * Contract to prove: `controller_transition` is total: every state/event pair has a deterministic result, and impossible states enter fault rather than guessing.
 */
