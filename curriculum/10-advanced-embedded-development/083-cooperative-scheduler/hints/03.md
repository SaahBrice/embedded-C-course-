# Hint 3 — Concrete route for Implement a Tiny Scheduler

Keep the assessed interface fixed and use wraparound to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
