#include "task.h"

/* TODO — Final Release: Portable Data Logger: implement the declared interface.
 * Contract to prove: The capstone release validates a versioned record shape, hashes exactly the serialized bytes, and accepts a record only when structure and integrity evidence both pass.
 */
