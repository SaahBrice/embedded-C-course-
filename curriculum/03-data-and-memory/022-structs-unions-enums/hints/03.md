# Hint 3 — Concrete route for Model a Sensor Packet

Keep the assessed interface fixed and use enumerations to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
