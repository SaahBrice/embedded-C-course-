#include "task.h"

/* TODO — Read a Linker Script: implement the declared interface.
 * Contract to prove: A section fits only when its complete address range lies within a linker memory region without overflow.
 */
