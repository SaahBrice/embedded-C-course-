#include "task.h"

/* TODO — Replace Blocking Delays: implement the declared interface.
 * Contract to prove: Each call performs bounded work, toggles only after one complete period, and remains correct when the tick counter wraps.
 */
