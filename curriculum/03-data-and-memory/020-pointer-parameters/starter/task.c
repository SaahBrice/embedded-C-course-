#include "task.h"

/* TODO — Return Results Through Pointers: implement the declared interface.
 * Contract to prove: The input bytes are read-only, the caller owns the output object, and no byte is read until length is sufficient.
 */
