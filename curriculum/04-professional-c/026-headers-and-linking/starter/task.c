#include "task.h"

/* TODO — Separate Interface from Implementation: implement the declared interface.
 * Contract to prove: `task.h` is self-contained and exposes one external declaration; the separate immutable harness is its consumer translation unit.
 */
