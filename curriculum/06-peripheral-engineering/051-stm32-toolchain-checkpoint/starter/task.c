#include "task.h"

/* TODO — Prepare the STM32 Toolchain: implement the declared interface.
 * Contract to prove: The compiled artifact is accepted only when its ELF magic and machine field identify a little-endian ARM target.
 */
