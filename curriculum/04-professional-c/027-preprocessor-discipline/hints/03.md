# Hint 3 — Concrete route for Use the Preprocessor Carefully

Keep the assessed interface fixed and use conditional compilation to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
