#include "task.h"

/* TODO — Inject Behavior with Callbacks: implement the declared interface.
 * Contract to prove: A registered callback is checked, invoked exactly once, and receives the caller-owned context unchanged.
 */
