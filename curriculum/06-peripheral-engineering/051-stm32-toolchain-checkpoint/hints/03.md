# Hint 3 — Concrete route for Prepare the STM32 Toolchain

Keep the assessed interface fixed and use Cube tools to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
