# Hint 3 — Concrete route for Map CPU and Memory Responsibilities

Keep the assessed interface fixed and use load/store to interpret the first compiler or checker diagnostic. Rerun `./learn test` after one focused change. The full worked implementation remains available through `./learn solution`.
