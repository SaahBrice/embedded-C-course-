#include "task.h"

/* TODO — Capstone: Export Reliably: implement the declared interface.
 * Contract to prove: Only temporary failures are retried, the bound is exact and observable, and permanent failure returns immediately.
 */
